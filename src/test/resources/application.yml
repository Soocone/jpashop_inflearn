spring:
#  datasource:
#    url: jdbc:h2:mem:testdb // h2를 메모리모드로 실행해주는 건데 설정해주지 않아도 테스트는 스프링부트가 알아서... 설정해줌 굿
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create-drop // 그리고 테스트에서는 create-drop 모드로 실행됨.
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true
#    open-in-view: false

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace